import { BaseTheme, ColorScheme } from "../../constants/theme";
export declare const createDarkTheme: (theme: BaseTheme) => ColorScheme;
export declare const createLightTheme: (theme: BaseTheme) => ColorScheme;
