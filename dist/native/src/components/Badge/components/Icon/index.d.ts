import { IconKeys } from "../../../BaseIcon";
export interface IconProps {
    name: IconKeys;
}
declare const Icon: ({ name }: IconProps) => import("react/jsx-runtime").JSX.Element;
export default Icon;
